package day13;
//collections->is a framework of dynamic data structure to store and manipulate the data in an efficient way. It provides various classes and interfaces to work with different types of collections such as lists, sets, maps, etc. Collections framework is a part of the Java standard library and is widely used in Java programming for handling data structures and algorithms.
//Iterable-> collection
/*  _______________________|________________________
   |                       |                        |
   list                  queue                     set
   |-> arrayList         |->deque                 |->hash set
   |-> linkedList                                 |->tree set
   |-> vector
   |-> stack
   */
// compare to array list have 
// Easy insertion ,deletion
//Dynamic resizing 
//inbuild method 
//stores wrapper class + Generics->Strict datatype checking
//List<> l=newList<>();
//l.add(10);
//l.add("word");//if we add List<Integer> l=new List<>(); then we can only add integer value in the list and if we try to add string value then it will give compilation error because of type mismatch
//int a=l.get(0);
//int b=l.get(1);
//ArrayList
 //->Ordered data
 //->index based access
 //->allows duplicate elements
//Methods
 //add(),remove(),removeALL(),set(),get(),size(),contains(),clear(),isEmpty(),indexOf(),lastIndexOf(),toArray() etc
public class collections {

    
}
